for (e = 0; e < E; e++)
  ...
  for (i = 0; i < I; i++)
    for (k = 0; k < K; k++)
      $t^0_k$ = $(z^0 a_{ik} + z^2 b_{ik})$
      $t^1_k$ = $(z^1 a_{ik} + z^3 b_{ik})$
    for (j = 0; j < J; j++)
      $t^2_j$ = $(z^0 c_{ij} + z^2 d_{ij})*W_i*det$
      $t^3_j$ = $(z^1 c_{ij} + z^3 d_{ij})*W_i*det$
    for (j = 0; j < J; j++)
      for (k = 0; k < K; k++)
        $a_{ejk}$ += $t^0_k * t^2_j$ + $t^1_k * t^3_j$
